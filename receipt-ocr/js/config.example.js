/**
 * Copy this file to config.js and set your Vision proxy URL.
 * config.js is gitignored — do not commit secrets (the proxy has credentials server-side).
 */
export const RECEIPT_OCR_API_BASE = 'http://localhost:8787';
