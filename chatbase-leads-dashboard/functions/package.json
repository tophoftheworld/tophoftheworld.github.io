{
  "name": "chatbase-conversations-dashboard-functions",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start",
    "deploy": "node ../scripts/prepare-deploy.mjs && firebase deploy --only functions:api,hosting",
    "lint": "echo \"No lint configured\"",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.2"
  }
}
