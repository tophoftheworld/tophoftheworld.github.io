{
  "name": "chatbase-leads-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "firebase emulators:start --only functions,firestore,hosting",
    "deploy": "firebase deploy --only functions,hosting,firestore:rules,firestore:indexes"
  }
}
